node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# Runtime data — never commit
data/
*.db
*.db-*
logs/
*.log
backups/

# Unscrubbed API captures. Only the output of `pnpm spike --scrub` belongs in the repo.
*-raw.json
*.raw.json

# Secrets / local config
.env
.env.*
!.env.example
*.pem
*.key

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Generated type declarations for the vendored Proton code (built by pnpm build:vendor).
vendor/proton/sieve/dist/


fixtures/

# Screenshots aus dem Testen. Bleiben lokal.
image*.png
